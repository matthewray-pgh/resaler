# Used by the Listing Generator tab (Claude API)
ANTHROPIC_API_KEY=

# Required in production — a long random string (e.g. `openssl rand -hex 32`)
SESSION_SECRET=

# Deployed frontend URL, so the backend's CORS allowlist matches it
CLIENT_ORIGIN=http://localhost:5173

NODE_ENV=development
PORT=3001
